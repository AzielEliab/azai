name: azai
description: AZAI companion. Ask Jeeves, receipts, integrity, seal. Constitutional edits blocked. Engine is the desktop package.
publish_to: "none"
version: 0.3.1+4

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
